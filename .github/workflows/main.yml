name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.1.0

      - name: Build and push Docker images api
        uses: docker/build-push-action@v2
        with:
          context: "./api"
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_SECRET }}
          repository: necromant/ffmpeg-jobs-api
          tags: latest
      - name: Build and push Docker images dashboard
        uses: docker/build-push-action@v2
        with:
          context: "./dashboard"
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_SECRET }}
          repository: necromant/ffmpeg-jobs-dashboard
          tags: latest
      - name: Build and push Docker images synchronizer
        uses: docker/build-push-action@v2
        with:
          context: "./synchronizer"
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_SECRET }}
          repository: necromant/ffmpeg-jobs-synchronizer
          tags: latest
      - name: Build and push Docker images worker
        uses: docker/build-push-action@v2
        with:
          context: "./worker"
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_SECRET }}
          repository: necromant/ffmpeg-jobs-worker
          tags: latest
